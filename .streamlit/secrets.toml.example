# Copy this file to .streamlit/secrets.toml and fill in your real key for local
# Streamlit runs, OR paste its contents into Streamlit Community Cloud's
# Settings -> Secrets when deploying. NEVER commit the real secrets.toml file --
# it's already excluded in .gitignore.

GEMINI_API_KEY = "your-gemini-api-key-here"
